import React, { useState } from 'react'
import configObject from '../config.js';

const PageImage = ({category}) => {
const [imageURL , setImageURL] = useState();

const res = fetch(`${configObject}/`)

return (
    <>

    </>
  )
}

export default PageImage
